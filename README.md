# CPP-YOLO-Traffic-Enforcement
A high-performance Computer Vision engine written in pure C++ that detects traffic light violations in real-time. By utilizing custom-trained YOLOv8 models exported to ONNX format, this system uses spatial logic (IoU) to determine if vehicles are illegally occupying a crosswalk during a red light.
